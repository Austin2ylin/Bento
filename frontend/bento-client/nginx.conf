server {
  # nginx-unprivileged 以非 root 執行，無法綁定 1024 以下的 port，改用 8080
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
