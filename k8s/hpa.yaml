apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bento-api-hpa
  namespace: bento
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bento-api
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
